/**
 * Stable public entry point used by src/App.tsx.
 *
 * The implementation lives in src/features/proteksi so the dashboard can be
 * maintained as small, focused modules without changing the application route.
 */
export { default } from "../features/proteksi/ProteksiDashboard";
